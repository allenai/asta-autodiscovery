[project]
name = "asta-agents"
version = "0.1.5"
description = "Add your description here"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Allen Institute for Artificial Intelligence" }
]
requires-python = ">=3.13"
dependencies = [
    "asta-sandbox[modal-ephemeral]",
    "asta-autodiscovery-modal",
    "google-adk>=1.28.1,<2",
    "litellm==1.83.7"
]

[build-system]
requires = ["uv_build>=0.8.16,<0.9.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "agents"
