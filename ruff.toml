target-version = "py314"
line-length = 100

[lint]
select = ["D", "E", "F", "UP", "B", "SIM", "I", "W", "Q", "ASYNC", "T20", "FA"]

[lint.pydocstyle]
convention = "google"

[lint.per-file-ignores]
"**/tests/*.py" = ["D"]
"**/report.py" = ["E501", "T20"]
